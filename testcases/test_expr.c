int main()
{
    // int a = 5 * (3 + 2);
    // int b = (a > 10) && (a < 30);
    // int c = b || (a % 2 == 0);
    int y;
    const int x = 10;
    x=20;

    // x = x * 5;
    // x = x + 2;

    return 0;
}