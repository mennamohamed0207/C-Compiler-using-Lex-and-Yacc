int main() {
    // Standard for loop
    for(int i = 0; i < 5; i=i+1) {
        // printf("Iteration: %d", i);
        int y=10;
    }

    for(int x = 0; x < 10; x=x+1) {
        // printf("X: %d, Y: %d", x, y);
        int z=10;
    }
    
    return 0;
}