int main() {
    int a = 10;
    
    // Simple if-else
    if (a > 5) {
        printf("A is greater than 5");
    } else {
        printf("A is 5 or less");
    }

    // Nested if
    if (a > 0) {
        if (a < 20) {
            printf("A is between 1-19");
        }
    }
    
    return 0;
}